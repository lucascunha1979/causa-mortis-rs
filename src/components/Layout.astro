---
import { Font } from "astro:assets";
import "../styles/global.css";
import Loader from "./Loader.astro";
import SpeedInsights from "@vercel/speed-insights/astro";

interface Props {
  title?: string;
  description?: string;
}

const defaultDescription =
  "Infográficos interativos sobre mortalidade no Rio Grande do Sul e seus municípios com dados oficiais do SIM/DATASUS, do Ministério da Saúde e do IBGE: evolução de taxas, causas de morte, distribuição geográfica e pirâmide de mortalidade por sexo e idade.";

const { title = "Causa Mortis RS", description = defaultDescription } =
  Astro.props;

const socialImage = new URL("/og-image.png", Astro.site);
const canonicalUrl = new URL(Astro.url.pathname, Astro.site);
---

<html lang="pt-BR" data-theme="dark" class="scroll-pt-16 scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <script is:inline>
      const storedTheme = localStorage.getItem("theme-preference");
      document.documentElement.dataset.theme =
        storedTheme === "light" ? "light" : "dark";
    </script>
    <link rel="icon" href="/favicon.ico" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="theme-color" content="#050a14" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <title>{title}</title>

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Causa Mortis" />
    <meta property="og:locale" content="pt_BR" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />

    <Font cssVariable="--font-sora" preload />
  </head>

  <body
    class="relative flex w-full flex-col overflow-x-hidden bg-white text-gray-900 dark:bg-gray-950 dark:text-gray-100"
  >
    <Loader />

    <slot />

    <SpeedInsights />
  </body>
</html>
